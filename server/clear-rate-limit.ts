/**
 * Clear Rate Limit Blocks
 * مسح حظر Rate Limit
 */

// This file can be imported to clear all rate limit blocks
export function clearAllRateLimits() {
  console.log('[RATE LIMIT] Clearing all blocks...');
  // The rateLimitStore is now disabled, so no action needed
  console.log('[RATE LIMIT] All blocks cleared!');
}
